apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service-direct.yaml
  - service-hls.yaml
  - ingress.yaml

configMapGenerator:
  - name: mediamtx-config
    files:
      - mediamtx.yml
